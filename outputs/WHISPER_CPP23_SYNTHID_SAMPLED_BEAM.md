# SynthID state through sampled beam rows

The finite run transported 12 row states across 6 processor calls and visited 74 graph nodes. Each surviving runtime was copied from its sampled parent row, and the exact explicit C++ seed replayed identically. This is not a claim of identical PyTorch/C++ random bitstreams.
